TROTEBOX 0.3.6 — TESTE LOCAL COMPLETO (WINDOWS POWERSHELL)

Com Supabase já configurado em apps/api/.env.local e packages/db/.env:

1. Abra um PowerShell:
cd "C:\Projetos\trote-box"

2. Execute UMA validação completa automatizada:
Set-ExecutionPolicy -Scope Process Bypass -Force
.\scripts\acceptance-local.ps1

Esse script executa preflight, quality:full, audit de produção, inicia Web/API temporariamente, aguarda as portas 3000/3001, executa smoke:local contra o banco configurado e encerra os servidores.

Resultado esperado:
TROTEBOX 0.3.6 - ACCEPTANCE APROVADA

Para apenas visualizar a interface sem backend, use INICIAR_PREVIEW_VISUAL.txt.
